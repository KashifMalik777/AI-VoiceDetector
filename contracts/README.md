# Contracts — FROZEN

Written once, on day 0, reviewed by all six in twenty minutes. **Read-only after that.**

Changing anything here requires everyone to agree, because every lane builds against it.
If you need something that isn't here: say so in the group, change it here, announce it.
Do not reach into someone else's directory.

- `schemas.json`   — every payload shape, machine-readable
- `ws-protocol.md` — the WebSocket message contract
- `fixtures/`      — realistic sample payloads; the mock server serves these verbatim
